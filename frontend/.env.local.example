BACKEND_URL=http://127.0.0.1:8000

# Only needed if the backend has STORAGE_API_KEY set: the Live screen lists
# every job, which that key protects. Read server-side, never sent to the browser.
# STORAGE_API_KEY=
